give me the command to install the vs code , chrome , meld , and c++ , c , python  

ChatGPT said:
ChatGPT
Here are the commands to install Visual Studio Code (VS Code), Google Chrome, Meld, and the C++, C, and Python development environments on Ubuntu 22.04.

1. Install Visual Studio Code:
bash
Copy
Edit
sudo apt update
sudo apt install software-properties-common apt-transport-https wget
wget -q https://packages.microsoft.com/keys/microsoft.asc -O- | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://packages.microsoft.com/repos/vscode stable main"
sudo apt update
sudo apt install code
2. Install Google Chrome:
bash
Copy
Edit
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo apt install ./google-chrome-stable_current_amd64.deb
3. Install Meld (Diff and Merge Tool):
bash
Copy
Edit
sudo apt update
sudo apt install meld
4. Install C and C++ Development Tools:
bash
Copy
Edit
sudo apt update
sudo apt install build-essential
5. Install Python:
Python is usually pre-installed on Ubuntu, but to ensure you have it, you can run:

bash
Copy
Edit
sudo apt update
sudo apt install python3 python3-pip
Su
